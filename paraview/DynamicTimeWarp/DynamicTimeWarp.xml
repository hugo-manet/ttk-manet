<?xml version="1.0" encoding="UTF-8"?>

<!-- TODO 11: Add widgets to the ParaView UI that control the member variables of the vtk filter -->

<!-- NOTE: Unfortunately the widget types and their properties are not well documented. -->
<!--       The best thing you can do is to look at filters that have similar widgets you require and copy their source code. -->
<!--       Good resources are: IcoSphere.xml, PersistenceDiagram.xml, and ArrayEditor.xml -->

<ServerManagerConfiguration>
    <ProxyGroup name="filters">
        <SourceProxy name="DynamicTimeWarp" class="ttkDynamicTimeWarp" label="TTK DynamicTimeWarp">
           <Documentation long_help="DynamicTimeWarp Long" short_help="DynamicTimeWarp Short">
               Computes a temporal map between two curves, which are presented as a matrix distance.
           </Documentation>

            <!-- INPUT -->
               <InputProperty name="Input" command="SetInputConnection">
                    <ProxyGroupDomain name="groups">
                        <Group name="sources" />
                        <Group name="filters" />
                    </ProxyGroupDomain>
                    <DataTypeDomain name="input_type">
                        <DataType value="vtkTable" />
                    </DataTypeDomain>
                    <InputArrayDomain name="input_scalars" number_of_components="1">
                        <Property name="Input" function="FieldDataSelection" />
                    </InputArrayDomain>
                    <Documentation>A distance matrix between the points of the two sequences.</Documentation>
                </InputProperty>

            <!-- INPUT PARAMETER WIDGETS -->

                <!-- This is the VTK way of selecting arrays for processing by calling the SetInputArrayToProcess method -->
                <!-- (see ttkDynamicTimeWarp.cpp for details) -->
                <!-- TODO check for deletion. Maybe replace with MatrixToHeatMap pipeline ?
                     <StringVectorProperty name="InputArray" label="Input Array" command="SetInputArrayToProcess" element_types="0 0 0 0 2" number_of_elements="5">
                    <ArrayListDomain attribute_type="Scalars" name="array_list">
                        <RequiredProperties>
                            <Property function="Input" name="Input" />
                        </RequiredProperties>
                    </ArrayListDomain>
                    <Documentation>The scalar array that will be used to compute averages.</Documentation>
                </StringVectorProperty>
                -->

                <!-- Create a UI group that contains all input parameter widgets (here only one) -->
                
                <!-- TODO same as above
                <PropertyGroup panel_widget="Line" label="Input Options">
                    <Property name="InputArray" />
                </PropertyGroup> -->

            <!-- ALGORITHM WIDGETS -->


                <DoubleVectorProperty name="DeletionCost" label="DeletionCost" command="SetDeletionCost" number_of_elements="1" default_values="1">
                    <Documentation>Cost for matching two consecutive diagrams to the same diagram in the other curve.</Documentation>
                    <DoubleRangeDomain name="range" min="0.5" max="1" />
                </DoubleVectorProperty>

                <PropertyGroup panel_widget="Line" label="Algorithm Options">
                    <Property name="DeletionCost">
                </PropertyGroup>

            <!-- OUTPUT PARAMETER WIDGETS -->

                <!-- A string parameter that controls the name of the output array -->
                <StringVectorProperty name="OutputPathName" label="Output Path Name" command="SetOutputPathName" number_of_elements="1" default_values="WarpingPath">
                </StringVectorProperty>

                <StringVectorProperty name="OutputMatchingName" label="Output Matching Name" command="SetOutputMatchingName" number_of_elements="1" default_values="TimeWarpMatching">
                </StringVectorProperty>

                <!-- Create a UI group that contains all output parameter widgets (here only one) -->
                <PropertyGroup panel_widget="Line" label="Output Options">
                    <Property name="OutputMatchingName" />
                    <Property name="OutputPathName" />
                </PropertyGroup>

            <!-- DEBUG -->
            ${DEBUG_WIDGETS}

            <!-- MENU CATEGORY -->
                <Hints>
                    <ShowInMenu category="TTK - Misc" />
                </Hints>
        </SourceProxy>
    </ProxyGroup>
</ServerManagerConfiguration>
